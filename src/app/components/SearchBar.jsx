import { IoSearch } from "react-icons/io5";

const SearchBar = () => {
  return (
    <section>
      <IoSearch></IoSearch>
      <input type="text" />
    </section>
  );
};

export default SearchBar;
